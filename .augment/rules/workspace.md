# Ever Works – Augment Workspace Rules

This file defines workspace-level rules for Augment Code.

- Project-wide AI instructions: see `CLAUDE.md` at the repository root.
- Multi-agent index and meta-rules: see `AGENTS.md` at the repository root.
- Human-facing documentation: https://docs.ever.works (source: `apps/docs/` + `docs/`)

To avoid duplication, do not restate project rules here; instead, keep this file as a thin pointer to the canonical sources above.
