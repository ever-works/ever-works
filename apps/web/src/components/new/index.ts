// Phase 6.5 PR CC2 — public surface of the web-side new/ directory.
// Houses the unified `/new` page client. PR DD repoints the sidebar
// "+ New Work" → "+ New" button at the route this serves.
export { NewPageClient, type ChipType } from './NewPageClient';
