fix(tenants-orgs): address PR #1042 bot findings (P2)

- user.entity.ts: drop redundant unique:true from username and slug
  decorators. Uniqueness is enforced only by the migration expression
  index; column-level UNIQUE would conflict (case-sensitive) with the
  case-insensitive expression index. Clearer comment too.
- username-allocator.service.ts: move randomBytes import to top of
  file, drop the runtime require + eslint-disable.

Codex P1 "check slug collisions in allocation loop" and Greptile P1
"allocateUsername omits slug collision check" were already addressed
in commit 55bdc58e (the collides refactor) -- those bot comments
predate that fix.

Codex P2 "normalize during slug backfill": acknowledged inline in the
migration. Platform is not live yet; allocator owns normalization for
all future writes; simple lower(username) backfill is safe.

Codex P2 "update slug when username changes": acknowledged in the
entity comment. Renames are out of scope for v1; future rename flow
lands in v1.1 and will explicitly update both columns.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
