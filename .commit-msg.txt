docs(specs): address PR #1043 second-pass bot findings

P1 (Greptile) -- spec/plan gap on tenantId backfill in Empty branch:
- spec.md sec 5.2: added a callout explaining the tenantId backfill
  lives inside OrganizationService.createOrganization (not the
  upgrade-from-account endpoint), so branch 3b also gets tenantId on
  existing rows.
- plan.md Phase 6: added step 5 to createOrganization spelling out
  the unconditional tenantId backfill.

P2 (Greptile) -- notarealsulug typo still in plan.md (I only fixed
acceptance.md the first time). Now fixed.

P2 (Greptile) -- ambiguous "earliest" criterion in first-Org guard:
clarified to "exactly one Org under this Tenant AND :organizationId
is that one Org id" with the explicit COUNT query.

P2 (Greptile) -- AC-2.2 did not cover lastScopeOrganizationId column:
updated to assert both tenantId IS NULL and lastScopeOrganizationId
IS NULL on every existing row.

P2 (Codex) -- Settings banner missing from phase checklist: added
explicit task in tasks.md Phase 9 components + wiring sections.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
