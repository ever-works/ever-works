interface:
  display_name: "Ever Works"
  short_description: "Repo-specific patterns and workflows for ever-works"
  default_prompt: "Use the ever-works repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true