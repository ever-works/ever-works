# NEGATIVE FIXTURE neg-08 — error class: secrecy does not propagate.
# Expected: JSON Schema PASS · rule layer FAIL `secret_reference_not_secret`.
# schema.md §21 "Secrecy propagates": an entry whose `from` names a secret output († `deps.postgres.url`)
# must itself be `secret: true`. §24.4 shows this exact code for this exact mistake. The schema cannot
# see it, because secrecy is a property of the REFERENCED output, not of the entry.
version: 2
kind: app
spec:
    source:
        relation: fork
        upstream: { repo: example-org/helpdesk, defaultBranch: main }
        branch: main
    build: { strategy: dockerfile }
    components:
        - { name: web, role: web, port: 3000 }
    dependencies:
        postgres: { version: '16' }
    env:
        - { name: DATABASE_URL, from: deps.postgres.url }
