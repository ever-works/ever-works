# NEGATIVE FIXTURE neg-04 — error class: `secret: true` with a literal `value`.
# Expected: JSON Schema FAIL (`if`/`then`: /spec/env/0 must NOT have `value`) · rule layer `literal_secret_value`.
# schema.md R8 / CONTRACTS.md C2: "A `secret: true` entry cannot carry `value`."
version: 2
kind: app
spec:
    source:
        relation: fork
        upstream: { repo: example-org/helpdesk, defaultBranch: main }
        branch: main
    build: { strategy: dockerfile }
    components:
        - { name: web, role: web, port: 3000 }
    env:
        - { name: SESSION_SECRET, secret: true, value: 'a-literal-secret-in-git' }
