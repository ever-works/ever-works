# NEGATIVE FIXTURE neg-16 — error class: a reference to a component that does not exist.
# Expected: JSON Schema PASS · rule layer FAIL `component_ref_unknown`.
# schema.md R14: "`jobs[].component`, `cron[].component`, `smoke[].component` name existing components".
# The component name is a `Name` either way; only the spec as a whole knows which names exist.
version: 2
kind: app
spec:
    source:
        relation: fork
        upstream: { repo: example-org/helpdesk, defaultBranch: main }
        branch: main
    build: { strategy: dockerfile }
    components:
        - { name: web, role: web, port: 3000 }
    jobs:
        - { name: migrate, when: pre-deploy, component: api, command: ['node', 'dist/migrate.js'] }
