# NEGATIVE FIXTURE neg-18 — error class: a literal secret in `build.args`.
# Expected: JSON Schema PASS · rule layer FAIL `literal_secret_in_build_args` (R10).
# This is §24.4's leading example: "Build arguments are stored in image layers. Reference an env entry with
# `fromEnv`." The schema sees `{ name: string, value: string }` — a secret is not a shape.
version: 2
kind: app
spec:
    source:
        relation: fork
        upstream: { repo: example-org/helpdesk, defaultBranch: main }
        branch: main
    build:
        strategy: dockerfile
        args:
            - { name: PAYMENTS_SECRET_KEY, value: 'sk_live_51H8xQ2eZvKYlo2C' }
    components:
        - { name: web, role: web, port: 3000 }
