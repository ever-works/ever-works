# GOLDEN — umami · target your-cluster · Secret `app-env-3289ab9d92`
# App spec  : docs/specs/features/app-works/APW-13-golden-paths/blueprints/umami/.works/works.yml (sha256 a2a7ba30b69e6fe55deebdbbddfd794623afcb59f23d57ff9bc89d39ed66f104)
# Renderer  : packages/plugins/k8s/src/app/ — APW-06 app-manifest.renderer.ts (T6), app-names.ts (T4),
#             app-security.ts (T5), app-jobs.renderer.ts / app-runner.script.ts (T8),
#             app-network-policy.renderer.ts (T7)
# Spec      : APW-06 plan.md §4.1 (name `app-env-<checksum first 10 hex>`, `immutable: true`)
# Spec      : APW-06 plan.md §4.7 (holds exactly the `values` from AppRuntimeEnvSource for phase runtime/both)
# Spec      : APW-06 spec.md FR-17 (a changed value restarts pods; an unchanged value never does)
# Spec      : Keys are EXACTLY the App spec env names whose `phase` is `runtime` (the default) or `both`.
# Spec      : Build-only names are excluded — see ../../README.md §"the build/runtime env split".
# Spec      : Values: literal for a non-secret `value:` entry the Blueprint states; `<…>` reference
# Spec      : placeholders everywhere else. Never a real secret.
# Pinned inputs and the placeholder convention: ../../README.md
# NO SECRET VALUE APPEARS IN THIS FILE — every secret is a reference or a generated placeholder.
apiVersion: v1
kind: Secret
metadata:
    name: app-env-3289ab9d92
    namespace: ew-umami-33333333
    labels:
        app.kubernetes.io/managed-by: ever-works-k8s-plugin
        app.kubernetes.io/part-of: umami
        ever-works.io/work-id: 33333333-3333-4333-8333-333333333333
        ever-works.io/kind: app
type: Opaque
immutable: true
stringData:
    DATABASE_URL: <deps.postgres.url>
    DIRECT_DATABASE_URL: <deps.postgres.directUrl>
    APP_SECRET: <generated:hex:32>
    TWO_FACTOR_ENCRYPTION_KEY: <generated:hex:32>
    DISABLE_TELEMETRY: '1'
    DISABLE_UPDATES: '1'
    APP_WEB_INTERNAL_URL: http://web.ew-umami-33333333.svc.cluster.local
    APP_BOOTSTRAP_ADMIN_PASSWORD: <prompted:APP_BOOTSTRAP_ADMIN_PASSWORD>
