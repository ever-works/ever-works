# Generated by Ever Works from .works/works.yml. Do not edit: hand edits are never overwritten;
# Ever Works proposes changes to this file as pull requests.
# ever-works-build generator=1 inputs=sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
#
# GOLDEN — cal-diy · `.github/workflows/ever-works-build.yml`
# App spec  : docs/specs/features/app-works/APW-13-golden-paths/blueprints/cal-diy/.works/works.yml (sha256 538a1389bc4dfc0f8fab31821d6e78aca2a0622d1f0e55fec1794433b646c16a)
# Generator : APW-05 plan.md §2.4 (normative sketch), §4.5 (emission), §4.14 + CONTRACTS §3 (R-9)
# Written by : packages/plugins/github-actions-build/src/workflow/generator.ts (T8)
# NO SECRET VALUE APPEARS IN THIS FILE — build values travel only as `EW_<NAME>` Actions
# secrets, referenced exclusively as `${{ secrets.EW_* }}` (APW-05 spec.md FR-10, FR-16).
# Placeholders: `inputsHash`, the five ACTION_PINS commit hashes and the image owner
# `example-owner` are documented placeholders — see ../README.md §"Values chosen".
# Two `run:` bodies are placeholders too: the specs describe their contract but own the text
# in the source files named inline.
name: Ever Works build
run-name: 'Ever Works build ${{ inputs.ew_build_id || github.event.pull_request.head.sha || github.sha }}'
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
  workflow_dispatch:
    inputs:
      ew_build_id:
        type: string
        required: true
      ew_sha:
        type: string
        required: true
      ew_mode:
        type: choice
        options:
          - build
          - verify
        default: build
      ew_verify_plan:
        type: string
        required: false
        default: ''
      ew_reuse_digest:
        type: string
        required: false
        default: ''
permissions: {}
concurrency:
  group: ${{ inputs.ew_mode == 'verify' && format('verify-{0}', inputs.ew_build_id) || format('ever-works-build-{0}', github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || github.ref_name) }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}
jobs:
  build:
    if: (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && inputs.ew_mode != 'verify'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      packages: write
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ever-works-build
          POSTGRES_PASSWORD: ever-works-build
          POSTGRES_DB: app
        ports:
          - 5432:5432
        options: '--health-cmd "pg_isready -U ever-works-build -d app"'
    env:
      EW_IMAGE: ghcr.io/example-owner/cal.diy/ever-works-app
      EW_SHA: ${{ inputs.ew_sha || github.event.pull_request.head.sha || github.sha }}
      EW_SECRET_NAMES: ''
    steps:
      # APW-05 plan.md §2.4 — prints names only; `EW_MISSING:<NAME>` then exit 78 on the first empty required value.
      # §4.7b: the `EW_MISSING` check runs only outside pull requests, which legitimately hold no stored value.
      - name: Check build values
        if: github.event_name != 'pull_request'
        env:
          EW_DATABASE_URL: ${{ secrets.EW_DATABASE_URL }}
        run: |
          set -uo pipefail
          for name in EW_DATABASE_URL; do
              if [ -z "${!name:-}" ]; then
                  echo "EW_MISSING:${name#EW_}"
                  exit 78
              fi
          done
          echo "Build values present: EW_DATABASE_URL"
      - uses: actions/checkout@1111111111111111111111111111111111111111 # v7
        with:
          ref: ${{ env.EW_SHA }}
          fetch-depth: 1
          persist-credentials: false
          lfs: false
      - name: Reclaim runner disk
        run: sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
      - uses: docker/setup-buildx-action@2222222222222222222222222222222222222222 # v4.3.0
        with:
          buildkitd-flags: '--allow-insecure-entitlement network.host'
      - uses: docker/login-action@3333333333333333333333333333333333333333 # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: build
        if: inputs.ew_mode != 'verify'
        uses: docker/build-push-action@4444444444444444444444444444444444444444 # v7.3.0
        with:
          context: .
          file: ./Dockerfile
          target: runner
          load: true
          push: false
          provenance: false
          network: host
          allow: network.host
          build-args: |
            MAX_OLD_SPACE_SIZE=6144
            CALCOM_TELEMETRY_DISABLED=1
            DATABASE_URL=${{ github.event_name == 'pull_request' && 'ew-restricted' || secrets.EW_DATABASE_URL }}
          tags: |
            ${{ env.EW_IMAGE }}:sha-${{ env.EW_SHA }}
            ${{ env.EW_IMAGE }}:${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || format('branch-{0}', 'main') }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ env.EW_SHA }}
            io.ever-works.app-spec-hash=538a1389bc4dfc0f8fab31821d6e78aca2a0622d1f0e55fec1794433b646c16a
          cache-from: 'type=registry,ref=${{ env.EW_IMAGE }}:buildcache'
          cache-to: ${{ github.event_name == 'push' && format('type=registry,ref={0}:buildcache,mode=max', env.EW_IMAGE) || '' }}
      - name: Push
        if: inputs.ew_mode != 'verify'
        run: 'docker push --all-tags "$EW_IMAGE" && docker image inspect --format ''{{index .RepoDigests 0}}'' "$EW_IMAGE:sha-$EW_SHA" > ew-digest.txt'
      # APW-05 plan.md §4.10 — the embedded verification runner. Its body is source code owned by
      # packages/plugins/github-actions-build/src/workflow/verify-runner.sh.ts; these specs describe the
      # contract (≤ 60,000-char base64url plan, ≤ 12 GiB summed memory, throwaway dependency containers,
      # a `0600` env file shredded on exit, per-job and per-smoke rows) but do not contain the script.
      - name: Verify in the runner
        if: inputs.ew_verify_plan != ''
        timeout-minutes: 30
        env:
          EW_VERIFY_PLAN: ${{ inputs.ew_verify_plan }}
          EW_VERIFY_PROMPTED: ${{ secrets.EW_VERIFY__PROMPTED }}
        run: '<verify-runner.sh.ts — the APW-05 plan.md §4.10 body. Not reproduced: the specs do not contain it.>'
      # APW-05 plan.md §2.4 — writes `ever-works-build-result.json` (≤ 8 KB) with schema, buildId, sha,
      # imageRepository, digest, tags, secretCheck and the verification rows. Body owned by
      # packages/plugins/github-actions-build/src/runs/result-artifact.ts.
      - name: Write result
        if: always()
        run: '<ever-works-build-result.json writer — keys: schema, buildId, sha, imageRepository, digest, tags, secretCheck, verification. Not reproduced: the specs do not contain the body.>'
      - uses: actions/upload-artifact@5555555555555555555555555555555555555555 # v4
        if: always()
        with:
          name: ever-works-build-result
          path: ever-works-build-result.json
          retention-days: 7
          if-no-files-found: ignore
  verify:
    if: github.event_name == 'workflow_dispatch' && inputs.ew_mode == 'verify'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      packages: read
    env:
      EW_IMAGE: ghcr.io/example-owner/cal.diy/ever-works-app
      EW_SHA: ${{ inputs.ew_sha }}
      EW_REUSE_DIGEST: ${{ inputs.ew_reuse_digest }}
      EW_VERIFY_BUILD: "<base64url of the plan's build section, when the plan carries one>"
    steps:
      - uses: actions/checkout@1111111111111111111111111111111111111111 # v7
        with:
          ref: ${{ inputs.ew_sha }}
          fetch-depth: 1
          persist-credentials: false
          lfs: false
      - uses: docker/setup-buildx-action@2222222222222222222222222222222222222222 # v4.3.0
        with:
          buildkitd-flags: '--allow-insecure-entitlement network.host'
      - uses: docker/login-action@3333333333333333333333333333333333333333 # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # APW-05 plan.md §2.4 — "if EW_REUSE_DIGEST is set: docker pull the confirmed image; otherwise
      # docker buildx build --load from the plan’s `build` section with --cache-from
      # \"$EW_IMAGE:buildcache\" and a local tag `ew-verify:local` — no --push, no cache-to."
      # The body is source code owned by src/workflow/verify-runner.sh.ts; the specs give the contract only.
      - name: Take the image to verify
        run: '<verify-runner.sh.ts — the APW-05 plan.md §2.4:260-263 body. Not reproduced: the specs do not contain it.>'
      # APW-05 plan.md §4.10 — the same embedded verification runner the build job uses.
      - name: Verify in the runner
        if: inputs.ew_verify_plan != ''
        timeout-minutes: 30
        env:
          EW_VERIFY_PLAN: ${{ inputs.ew_verify_plan }}
          EW_VERIFY_PROMPTED: ${{ secrets.EW_VERIFY__PROMPTED }}
        run: '<verify-runner.sh.ts — the APW-05 plan.md §4.10 body. Not reproduced: the specs do not contain it.>'
      # APW-05 plan.md §2.4:268-271 — schema, buildId, sha, imageRepository, reusedDigest | localBuild,
      # verify results; no digest claim, no tags. Body owned by src/runs/result-artifact.ts.
      - name: Write result
        if: always()
        run: '<ever-works-build-result.json writer (verify variant) — keys: schema, buildId, sha, imageRepository, reusedDigest | localBuild, verification. Not reproduced: the specs do not contain the body.>'
      - uses: actions/upload-artifact@5555555555555555555555555555555555555555 # v4
        if: always()
        with:
          name: ever-works-build-result
          path: ever-works-build-result.json
          retention-days: 7
          if-no-files-found: ignore
  checks:
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    name: 'Ever Works check: ${{ matrix.check.name }}'
    runs-on: ubuntu-latest
    timeout-minutes: ${{ matrix.check.timeoutMinutes }}
    continue-on-error: ${{ !matrix.check.required }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        check:
          - name: type-check
            required: true
            timeoutMinutes: 60
            commandB64: eWFybiBpbnN0YWxsIC0taW1tdXRhYmxlICYmIHlhcm4gdHlwZS1jaGVjazpjaSAtLWZvcmNl
          - name: biome
            required: false
            timeoutMinutes: 30
            commandB64: eWFybiBpbnN0YWxsIC0taW1tdXRhYmxlICYmIHlhcm4gYmlvbWUgY2hlY2sgLg==
          - name: unit-tests
            required: false
            timeoutMinutes: 60
            commandB64: eWFybiBpbnN0YWxsIC0taW1tdXRhYmxlICYmIFRaPVVUQyB5YXJuIHRlc3Q=
    steps:
      - uses: actions/checkout@1111111111111111111111111111111111111111 # v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 1
          persist-credentials: false
          lfs: false
      - name: Run check
        env:
          EW_CHECK_COMMAND_B64: ${{ matrix.check.commandB64 }}
        run: printf '%s' "$EW_CHECK_COMMAND_B64" | base64 -d > "$RUNNER_TEMP/ew-check.sh" && bash -e "$RUNNER_TEMP/ew-check.sh"
