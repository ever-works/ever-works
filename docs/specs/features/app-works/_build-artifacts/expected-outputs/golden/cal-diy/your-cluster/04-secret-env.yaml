# GOLDEN — cal-diy · target your-cluster · Secret `app-env-b1eff0d86e`
# App spec  : docs/specs/features/app-works/APW-13-golden-paths/blueprints/cal-diy/.works/works.yml (sha256 538a1389bc4dfc0f8fab31821d6e78aca2a0622d1f0e55fec1794433b646c16a)
# Renderer  : packages/plugins/k8s/src/app/ — APW-06 app-manifest.renderer.ts (T6), app-names.ts (T4),
#             app-security.ts (T5), app-jobs.renderer.ts / app-runner.script.ts (T8),
#             app-network-policy.renderer.ts (T7)
# Spec      : APW-06 plan.md §4.1 (name `app-env-<checksum first 10 hex>`, `immutable: true`)
# Spec      : APW-06 plan.md §4.7 (holds exactly the `values` from AppRuntimeEnvSource for phase runtime/both)
# Spec      : APW-06 spec.md FR-17 (a changed value restarts pods; an unchanged value never does)
# Spec      : Keys are EXACTLY the App spec env names whose `phase` is `runtime` (the default) or `both`.
# Spec      : Build-only names are excluded — see ../../README.md §"the build/runtime env split".
# Spec      : Values: literal for a non-secret `value:` entry the Blueprint states; `<…>` reference
# Spec      : placeholders everywhere else. Never a real secret.
# Pinned inputs and the placeholder convention: ../../README.md
# NO SECRET VALUE APPEARS IN THIS FILE — every secret is a reference or a generated placeholder.
apiVersion: v1
kind: Secret
metadata:
    name: app-env-b1eff0d86e
    namespace: ew-cal-diy-11111111
    labels:
        app.kubernetes.io/managed-by: ever-works-k8s-plugin
        app.kubernetes.io/part-of: cal-diy
        ever-works.io/work-id: 11111111-1111-4111-8111-111111111111
        ever-works.io/kind: app
type: Opaque
immutable: true
stringData:
    NEXTAUTH_SECRET: <generated:base64:32>
    CALENDSO_ENCRYPTION_KEY: <generated:chars:32>
    CRON_API_KEY: <generated:hex:32>
    CRON_SECRET: <generated:hex:32>
    DATABASE_URL: <deps.postgres.url>
    DATABASE_DIRECT_URL: <deps.postgres.directUrl>
    DATABASE_HOST: '<template:{{deps.postgres.host}}:{{deps.postgres.port}}>'
    NEXT_PUBLIC_WEBAPP_URL: https://cal-diy.ever.works
    NEXTAUTH_URL: '<template:{{domains.primary.url}}/api/auth>'
    NEXTAUTH_URL_INTERNAL: '<template:{{components.web.internalUrl}}/api/auth>'
    APP_WEB_INTERNAL_URL: http://web.ew-cal-diy-11111111.svc.cluster.local
    EMAIL_SERVER_HOST: <deps.smtp.host>
    EMAIL_SERVER_PORT: <deps.smtp.port>
    EMAIL_SERVER_USER: <deps.smtp.user>
    EMAIL_SERVER_PASSWORD: <deps.smtp.password>
    EMAIL_FROM: <deps.smtp.from>
    EMAIL_FROM_NAME: Cal.diy (community build)
    CALCOM_TELEMETRY_DISABLED: '1'
    TURBO_TELEMETRY_DISABLED: '1'
    TZ: UTC
    GOOGLE_LOGIN_ENABLED: 'false'
    APP_BOOTSTRAP_ADMIN_EMAIL: <prompted:APP_BOOTSTRAP_ADMIN_EMAIL>
    APP_BOOTSTRAP_ADMIN_PASSWORD: <prompted:APP_BOOTSTRAP_ADMIN_PASSWORD>
    GOOGLE_API_CREDENTIALS: <prompted:GOOGLE_API_CREDENTIALS>
    GOOGLE_WEBHOOK_TOKEN: <generated:hex:32>
