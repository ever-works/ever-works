# GOLDEN RENDERED OUTPUT — app-fixture-hello. ServiceAccount `app` (APW-06 plan §4.1, §4.3).
# `automountServiceAccountToken: false` and no RoleBinding: the fixture's own endpoints never touch the
# Kubernetes API, and ACC-E2E-05 asserts no cluster credential is mounted (APW-06 spec FR-12).
# Identical to cal-diy's — the App renderer's ServiceAccount is target-neutral.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: app
  namespace: ew-app-fixture-hello-22222222
  labels:
    app.kubernetes.io/managed-by: ever-works-k8s-plugin
    app.kubernetes.io/part-of: app-fixture-hello
    ever-works.io/work-id: 22222222-2222-4222-8222-222222222222
    ever-works.io/kind: app
automountServiceAccountToken: false
