# GOLDEN RENDERED OUTPUT — app-fixture-hello. Env Secret `app-env-fa90df132e` (APW-06 plan §4.1, §4.7).
# Immutable; keys are EXACTLY the runtime/both-phase env names. ACC-E2E-05 asserts the key set equals the
# App spec's run-phase env names and that the BUILD-ONLY name `FIXTURE_BUILD_LABEL` is excluded
# (ACCEPTANCE.md:335-338) — `FIXTURE_BUILD_LABEL` is `phase: build` (works.yml:91), so it never appears here.
#
# 🛑 EVERY VALUE BELOW IS THE LITERAL PLACEHOLDER `<redacted>`. The assertion surface is the key set.
# ACC-NEG-12 additionally asserts that a generated value is IDENTICAL across redeploy, rebuild, restart,
# App-spec re-apply and upstream sync — the fixture proves it externally through `GET /state`'s
# `secretFingerprint` (length + hash prefix, never the value).
apiVersion: v1
kind: Secret
metadata:
  name: app-env-fa90df132e
  namespace: ew-app-fixture-hello-22222222
  labels:
    app.kubernetes.io/managed-by: ever-works-k8s-plugin
    app.kubernetes.io/part-of: app-fixture-hello
    ever-works.io/work-id: 22222222-2222-4222-8222-222222222222
    ever-works.io/kind: app
type: Opaque
immutable: true
stringData:
  DATABASE_URL: "<redacted>"
  FIXTURE_CRON_TOKEN: "<redacted>"
  FIXTURE_GIT_SHA: "<redacted>"
  FIXTURE_INTERNAL_URL: "<redacted>"
  FIXTURE_MAIL_TO: "<redacted>"
  FIXTURE_MARKER: "<redacted>"
  FIXTURE_PUBLIC_URL: "<redacted>"
  FIXTURE_SESSION_SECRET: "<redacted>"
  SMTP_FROM: "<redacted>"
  SMTP_HOST: "<redacted>"
  SMTP_PASSWORD: "<redacted>"
  SMTP_PORT: "<redacted>"
  SMTP_USER: "<redacted>"
