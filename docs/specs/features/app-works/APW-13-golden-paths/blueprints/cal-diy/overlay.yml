# Overlay manifest — APW-03 catalog.md §5. One row per file under `overlay/`, and one file per row.
#
# EMPTY ON PURPOSE: this Blueprint adds nothing to the Work Repository's tree. It builds the upstream's own
# Dockerfile at `target: runner` and configures the app entirely through App spec env entries, jobs and crons, so
# there is no overlay file to copy and no `overlay/` directory. A fork-side non-root Dockerfile (see README.md,
# "Unverified") would be the first entry here.
#
# Row shape for future entries:
#   files:
#       - path: Dockerfile # destination in the Work Repository, relative
#         mode: add-only # add-only (skip when the file exists) · replace (only through a pull request)

files: []
