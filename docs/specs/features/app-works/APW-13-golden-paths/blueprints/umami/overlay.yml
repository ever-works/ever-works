# Overlay manifest — APW-03 catalog.md §5. One row per file under `overlay/`, and one file per row.
#
# EMPTY ON PURPOSE: this Blueprint deploys the upstream's published image pinned by digest and adds nothing to the
# Work Repository's tree, so there is no overlay file to copy and no `overlay/` directory. Switching
# `build.strategy` to `dockerfile` also needs no overlay — the upstream ships its own multi-stage Dockerfile.
#
# Row shape for future entries:
#   files:
#       - path: Dockerfile # destination in the Work Repository, relative
#         mode: add-only # add-only (skip when the file exists) · replace (only through a pull request)

files: []
