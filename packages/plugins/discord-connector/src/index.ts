/**
 * @ever-works/discord-connector-plugin — Discord connector (bidirectional).
 * Outbound channel-message post via the `discord.js` REST client; inbound
 * Interactions API routing lands in a follow-up. See
 * `docs/specs/features/connectors/spec.md` §7.5.1.
 */
export { DiscordConnectorPlugin, discordConnectorPlugin } from './discord-connector-plugin.js';
