/**
 * @ever-works/mailchimp-transactional-plugin — Mailchimp Transactional
 * (Mandrill) email provider (outbound).
 *
 * This is the transactional product (mandrillapp.com per-message API),
 * NOT the Mailchimp Marketing/campaigns API. See
 * `docs/specs/features/email-providers/spec.md` §6.
 */
export { MailchimpTransactionalPlugin, mailchimpTransactionalPlugin } from './mailchimp-transactional-plugin.js';
