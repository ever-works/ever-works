/**
 * Help centre (AW-25) — in-product manual contracts.
 *
 * Closed vocabularies and the structured article grammar shared by the
 * build-time generator and every client that renders the manual. Pure types
 * plus literal tuples, importable from server and client code alike.
 */
export * from './help.enum.js';
