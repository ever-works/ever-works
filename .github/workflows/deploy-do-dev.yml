name: Deploy to DO Dev

on:
  workflow_run:
    workflows: ['Build and Publish Docker Images Dev']
    branches: [develop]
    types:
      - completed

jobs:
  deploy-dev:
    runs-on: ubuntu-latest

    environment:
      name: dev
      url: https://appdev.ever.works

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Save DigitalOcean kubeconfig with short-lived credentials
        run: doctl kubernetes cluster kubeconfig save --expiry-seconds 600 k8s-gauzy

      - name: Write PostgreSQL Certificate file
        run: |
          echo "$DB_CA_CERT" | base64 --decode > ${HOME}/ca-certificate.crt
        env:
          DB_CA_CERT: '${{ secrets.DATABASE_CA_CERT }}'

      - name: Generate TLS Secrets
        run: |
          rm -f ${HOME}/ingress.api.crt ${HOME}/ingress.api.key ${HOME}/ingress.webapp.crt ${HOME}/ingress.webapp.key
          echo ${{ secrets.INGRESS_API_CERT }} | base64 --decode > ${HOME}/ingress.api.crt
          echo ${{ secrets.INGRESS_API_CERT_KEY }} | base64 --decode > ${HOME}/ingress.api.key
          echo ${{ secrets.INGRESS_WEBAPP_CERT }} | base64 --decode > ${HOME}/ingress.webapp.crt
          echo ${{ secrets.INGRESS_WEBAPP_CERT_KEY }} | base64 --decode > ${HOME}/ingress.webapp.key
          kubectl create secret tls apidev.ever.works-tls --save-config --dry-run=client --cert=${HOME}/ingress.api.crt --key=${HOME}/ingress.api.key -o yaml | kubectl --context do-sfo2-k8s-gauzy apply -f -
          kubectl create secret tls appdev.ever.works-tls --save-config --dry-run=client --cert=${HOME}/ingress.webapp.crt --key=${HOME}/ingress.webapp.key -o yaml | kubectl --context do-sfo2-k8s-gauzy apply -f -

      - name: Apply k8s manifests changes in DigitalOcean k8s cluster (if any)
        run: |
          envsubst < $GITHUB_WORKSPACE/.deploy/k8s/k8s-manifest.dev.yaml | kubectl --context do-sfo2-k8s-gauzy apply -f -
        env:
          # API ENV
          AUTH_SECRET: ${{ secrets.AUTH_SECRET }}
          WEB_URL: https://appdev.ever.works
          ALLOWED_ORIGINS: https://appdev.ever.works,https://apidev.ever.works
          JWT_SECRET: ${{ secrets.JWT_SECRET }}

          # Trigger.dev
          TRIGGER_ENABLED: ${{ secrets.TRIGGER_ENABLED }}
          TRIGGER_SECRET_KEY: ${{ secrets.TRIGGER_SECRET_KEY }}
          TRIGGER_INTERNAL_SECRET: ${{ secrets.TRIGGER_INTERNAL_SECRET }}
          TRIGGER_MACHINE: ${{ secrets.TRIGGER_MACHINE }}

          # GitHub Auth
          GH_CLIENT_ID: ${{ secrets.GH_CLIENT_ID }}
          GH_CLIENT_SECRET: ${{ secrets.GH_CLIENT_SECRET }}
          GH_CALLBACK_URL: https://appdev.ever.works/api/oauth/github/callback

          # Google Auth
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_CALLBACK_URL: https://appdev.ever.works/api/oauth/google/callback

          # Facebook Auth
          FACEBOOK_CLIENT_ID: ${{ secrets.FACEBOOK_CLIENT_ID }}
          FACEBOOK_CLIENT_SECRET: ${{ secrets.FACEBOOK_CLIENT_SECRET }}
          FACEBOOK_CALLBACK_URL: https://appdev.ever.works/api/oauth/facebook/callback

          # LinkedIn Auth
          LINKEDIN_CLIENT_ID: ${{ secrets.LINKEDIN_CLIENT_ID }}
          LINKEDIN_CLIENT_SECRET: ${{ secrets.LINKEDIN_CLIENT_SECRET }}
          LINKEDIN_CALLBACK_URL: https://appdev.ever.works/api/oauth/linkedin/callback

          # Plugins
          PLUGIN_OPENROUTER_API_KEY: ${{ secrets.PLUGIN_OPENROUTER_API_KEY }}
          PLUGIN_OPENROUTER_DEFAULT_MODEL: ${{ secrets.PLUGIN_OPENROUTER_DEFAULT_MODEL }}
          PLUGIN_OPENROUTER_SIMPLE_MODEL: ${{ secrets.PLUGIN_OPENROUTER_SIMPLE_MODEL }}
          PLUGIN_OPENROUTER_MEDIUM_MODEL: ${{ secrets.PLUGIN_OPENROUTER_MEDIUM_MODEL }}
          PLUGIN_OPENROUTER_COMPLEX_MODEL: ${{ secrets.PLUGIN_OPENROUTER_COMPLEX_MODEL }}
          PLUGIN_GITHUB_CLIENT_ID: ${{ secrets.PLUGIN_GITHUB_CLIENT_ID }}
          PLUGIN_GITHUB_CLIENT_SECRET: ${{ secrets.PLUGIN_GITHUB_CLIENT_SECRET }}
          PLUGIN_TAVILY_API_KEY: ${{ secrets.PLUGIN_TAVILY_API_KEY }}
          PLUGIN_SCREENSHOTONE_ACCESS_KEY: ${{ secrets.PLUGIN_SCREENSHOTONE_ACCESS_KEY }}
          PLUGIN_SCREENSHOTONE_SECRET_KEY: ${{ secrets.PLUGIN_SCREENSHOTONE_SECRET_KEY }}

          # Database
          DATABASE_TYPE: ${{ secrets.DATABASE_TYPE }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DATABASE_HOST: ${{ secrets.DATABASE_HOST }}
          DATABASE_PORT: ${{ secrets.DATABASE_PORT }}
          DATABASE_USERNAME: ${{ secrets.DATABASE_USERNAME }}
          DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
          DATABASE_NAME: ${{ secrets.DATABASE_NAME }}
          # Enable SSL/TLS for database connections (true/false)
          DATABASE_SSL_MODE: ${{ secrets.DATABASE_SSL_MODE }}
          DATABASE_CA_CERT: ${{ secrets.DATABASE_CA_CERT }}

          # Mail
          MAILER_PROVIDER: ${{ secrets.MAILER_PROVIDER }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}

          # SMTP
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_SECURE: ${{ secrets.SMTP_SECURE }}
          SMTP_IGNORE_TLS: ${{ secrets.SMTP_IGNORE_TLS }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}

          # Resend
          RESEND_APIKEY: ${{ secrets.RESEND_APIKEY }}
          RESEND_EMAIL_FROM: ${{ secrets.RESEND_EMAIL_FROM }}

      # we need this step because for now we just use :latest tag
      # note: for production we will use different strategy later
      - name: Restart Pods to pick up :latest tag version
        run: |
          kubectl --context do-sfo2-k8s-gauzy rollout restart deployment/ever-works-api-dev
          kubectl --context do-sfo2-k8s-gauzy rollout restart deployment/ever-works-web-dev
